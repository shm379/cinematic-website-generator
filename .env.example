# Cinemate — environment variables (all optional)
# Copy to .env for local dev, or set these in Coolify's environment settings.

# Port the server binds to (Coolify/Docker set this; defaults to 3000).
# PORT=3000

# Card images — POST /api/image.
# Primary source: Pexels stock photos. Set your Pexels API key (get one free at
# https://www.pexels.com/api/) and the "گرفتن عکس" buttons pull curated photos.
# PEXELS_API_KEY=...
#
# Optional fallback: OpenAI gpt-image generation. Used only when PEXELS_API_KEY
# is absent, or when a Pexels search fails. Without either key, the buttons
# return a friendly "configure a key" message.
# OPENAI_API_KEY=sk-...
# OPENAI_IMAGE_MODEL=gpt-image-1

# Mega-prompt → site — POST /api/generate-from-prompt.
# With this key the prompt is parsed by Claude; without it, a keyless
# heuristic parser is used instead (so the feature always works).
# ANTHROPIC_API_KEY=sk-ant-...
# CLAUDE_MODEL=claude-opus-4-8
